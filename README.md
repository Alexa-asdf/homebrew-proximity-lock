# Homebrew Tap for Proximity Lock

Install Proximity Lock with:

```zsh
brew install Alexa-asdf/proximity-lock/proximity-lock
```

The formula is prepared for the v0.1.0 beta release. Its release archive and
Python dependency checksums must be verified and added before Homebrew
installation is enabled.
